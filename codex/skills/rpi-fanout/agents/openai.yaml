interface:
  display_name: "RPI Fanout"
  short_description: "Organize a requested corpus analysis as workers"
  default_prompt: "Use $rpi-fanout to analyze this corpus with worker, critic, and synthesis stages."
