interface:
  display_name: "RPI Review"
  short_description: "Review code, resolve findings, and validate coverage"
  default_prompt: "Use $rpi-review to review this change and its acceptance coverage."
