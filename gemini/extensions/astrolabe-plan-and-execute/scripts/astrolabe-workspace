#!/usr/bin/env bash
# Resolve and ensure the working-tree directory execution uses for its
# short-lived artifacts: task briefs, implementor reports, and review packages.
# Print the directory's absolute path.
#
# The workspace lives in the working tree (not under .git/) because Claude Code
# treats .git/ as a protected path and denies agent writes there — which blocks
# an implementor subagent from writing its report file. A self-ignoring
# .gitignore keeps the workspace out of `git status` and out of accidental
# commits without modifying any tracked file.
#
# It sits under .astrolabe/exec/ rather than .astrolabe/ directly so it cannot shadow or
# ignore the tracked context files (.astrolabe/CONTEXT.md, .astrolabe/PROJECT.md).
#
# Single source of truth for the workspace location, so task-brief and
# review-package cannot drift to different directories.
#
# Usage: astrolabe-workspace
set -euo pipefail

root=$(git rev-parse --show-toplevel)
dir="$root/.astrolabe/exec"
mkdir -p "$dir"
printf '*\n' > "$dir/.gitignore"
cd "$dir" && pwd
