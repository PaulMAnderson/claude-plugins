#!/usr/bin/env bash
# BeforeModel hook - skill reinforcement
# This script can be used to inject context before each model turn.
# Currently disabled (exit 0) to match the Claude version's recent change.

echo '{"decision": "allow"}'
exit 0
