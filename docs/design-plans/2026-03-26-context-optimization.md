# Context Optimization and Automated Re-hydration Design

## Summary
This project implements a "Hot/Cold" context management system designed to maintain high performance in long-running agent sessions without exceeding token limits. The core strategy involves aggressively pruning active `SKILL.md` instructions down to a "Hot" core of mandatory constraints while offloading detailed "Cold" domain knowledge into secondary `REFERENCE.md` files. These reference files are only loaded by the agent when a specific task requires that domain expertise, significantly reducing the baseline prompt overhead and freeing up context for complex reasoning.

To ensure continuity across mandatory context resets, the system introduces manual session journaling. A user-invocable skill captures critical milestones—such as file modifications, architectural decisions, and phase completions—into a human-readable journal. When a session is cleared (either manually via `/clear` or automatically due to context pressure), a startup hook identifies the most recent journal entry and re-seeds the new session with its essential state. This allows the agent to resume work with a full understanding of the project's intent and current progress, effectively eliminating the "amnesia" that typically follows a context wipe.

## 🗂 Memory Tier Index

| Tier | Sections | When to read |
|------|----------|-------------|
| 🔴 **HOT** | DoD, Acceptance Criteria | Always — load before doing anything |
| 🟡 **WARM** | Architecture, Implementation Phases | When planning or implementing a specific phase |
| 🔵 **COLD** | Existing Patterns, Additional Considerations, Glossary | Reference on demand only |

## 🔴 Definition of Done
- All `SKILL.md` files in the `plugins/` directory are refactored to be under the 150-instruction limit by moving general knowledge to separate `REFERENCE.md` files.
- A machine-readable `.rpi/journal.json` and human-readable `.rpi/SESSION.md` are automatically maintained by a `PostToolUse` hook.
- The `SessionStart` hook automatically re-seeds the context from `.rpi/journal.json` after a `/clear`.
- A standardized `Implementation Registry` is added to all implementation plan files to track phase progress (`[ ]`, `[/]`, `[x]`).
- New `/status` and `/resume` (aliased or renamed if conflict exists) commands provide session progress summaries and manual re-hydration triggers.
- No information loss occurs across at least three consecutive `/clear` and re-hydration cycles during a complex task.

## 🔴 Acceptance Criteria

### context-optimization.AC1: Modular Skill Refactoring
- **context-optimization.AC1.1 Success:** `howto-code-in-typescript/SKILL.md` is reduced from ~780 lines to <150 lines.
- **context-optimization.AC1.2 Success:** Core house style rules remain in `SKILL.md`, while general programming advice moves to `REFERENCE.md`.
- **context-optimization.AC1.3 Success:** Agent correctly invokes `read_file` on `REFERENCE.md` when tasked with a specific TypeScript refactoring.

### context-optimization.AC4: Implementation Registry
- **context-optimization.AC4.1 Success:** New implementation plans generated by `starting-an-implementation-plan` contain a status table for all phases.
- **context-optimization.AC4.2 Success:** Updating a phase marker to `[x]` in the implementation plan file is reflected in the `/status` command output.
- **context-optimization.AC4.3 Success:** The `/status` command provides a concise summary of current session goals vs. overall project progress.

## 🟡 Architecture

The system uses a "Hot/Cold" prompt strategy combined with "Journaled Checkpointing" to manage context pressure.

### Modular Skill Refactoring (Hot/Cold)
- **Hot Prompt:** `SKILL.md` files are limited to <150 instructions, focusing on active phase directives and mandatory constraints.
- **Cold Storage:** General programming knowledge and detailed house style rules (e.g., TypeScript conventions) are moved to `REFERENCE.md` files.
- **On-Demand Loading:** Agents are instructed to only `read_file` `REFERENCE.md` files when a specific task requires that domain knowledge.

### Implementation Registry
- Every implementation plan file (`docs/implementation-plans/*.md`) includes a standardized status table.
- Agents are required to update these markers (`[ ]`, `[/]`, `[x]`) as their primary task completion signal.

## 🔵 Existing Patterns

- **`.rpi/` directory:** Already used for `CONTEXT.md`. This design extends its usage for human-readable `SESSION.md`.
- **`SessionStart` hook:** Currently used in `rpi-plan-and-execute/hooks/session-start.sh` to inject baseline skills.
- **Phase Handoffs:** Existing skills like `starting-a-design-plan` already mention `/clear`.

## 🟡 Implementation Phases

<!-- START_PHASE_1 -->
### Phase 1: Modular Skill Refactoring (Hot/Cold)
**Goal:** Reduce baseline prompt overhead by splitting the largest skills.

**Components:**
- `plugins/rpi-house-style/skills/howto-code-in-typescript/SKILL.md` and `REFERENCE.md`
- `plugins/rpi-plan-and-execute/skills/writing-implementation-plans/SKILL.md` and `REFERENCE.md`
- `plugins/rpi-plan-and-execute/skills/writing-design-plans/SKILL.md` and `REFERENCE.md`

**Dependencies:** None

**Done when:** All three skills are <150 instructions; general knowledge is moved to REFERENCE files; agents successfully load REFERENCE files on-demand in tests.
<!-- END_PHASE_1 -->

<!-- START_PHASE_4 -->
### Phase 4: Implementation Registry & Status Commands
**Goal:** Track long-term progress across phase boundaries.

**Components:**
- `plugins/rpi-plan-and-execute/skills/starting-an-implementation-plan/SKILL.md` — upgraded to initialize status markers
- `plugins/rpi-plan-and-execute/commands/status.md` (new) — displays session/registry status
- `plugins/rpi-plan-and-execute/commands/resume.md` (new) — manual re-hydration trigger

**Dependencies:** Phase 3

**Done when:** Implementation plans have working status markers; `/status` correctly reflects progress; `/resume` successfully re-loads a stale session.
<!-- END_PHASE_4 -->

## 🔵 Additional Considerations

- **Conflict with `/resume`:** Claude Code may already have a `/resume` command. If so, we will use `/rpi-resume` or `/continue`.
- **JSON Stability:** The `journal.json` structure must be simple and append-only where possible to avoid corruption.
- **Hook Latency:** The `PostToolUse` hook must be extremely fast to avoid adding lag to every tool interaction. We will use a lightweight shell script or compiled binary if needed.

## 🔵 Glossary
- **SKILL.md**: A file containing the primary instructions and constraints that define an agent's specific capability or "skill."
- **REFERENCE.md**: A supplemental file used for "Cold Storage" of general knowledge, house style rules, or detailed documentation, intended to be read only when required by the task.
- **Hot/Cold Prompt Strategy**: A context management pattern that keeps essential, frequently used instructions in the active prompt ("Hot") while moving auxiliary information to external files ("Cold").
- **Journaled Checkpointing**: The manual process of saving session state (intents, file changes, and decisions) to a local file.
- **SessionStart Hook**: A script executed at the beginning of a new session or after a context wipe, responsible for initializing the environment.
- **Implementation Registry**: A standardized status table embedded in implementation plans to track the completion state (`[ ]`, `[/]`, `[x]`) of various project phases.
- **additionalContext**: A technical mechanism used by the agent's runtime to inject external data or previous session state into the initial conversation prompt.
- **/clear**: A CLI command that resets the agent's conversation history to free up context window space, which usually results in the loss of session state.
