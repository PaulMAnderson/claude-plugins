# Helper Function

Use this command for adding single helper functions or small utilities that don't require persistent design plans or multi-phase implementation.

## Usage

1. Call `/helper-function`.
2. Provide details about the function's inputs, outputs, and requirements.
3. The agent will load the `helper-function` skill to guide the execution.
