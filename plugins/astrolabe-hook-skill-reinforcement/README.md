# Astrolabe skill reinforcement hook for Claude Code

This plugin retains a `UserPromptSubmit` hook for compatibility. Its current script exits without injecting a reminder; the per-message reminder was removed to avoid repeated context usage. Skill selection still follows the installed skills and project instructions.

See the [root README](../../README.md) for the workflow plugins and installation.
