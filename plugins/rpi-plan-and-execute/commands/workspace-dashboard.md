# Workspace Dashboard

Use this command to view the central workspace dashboard summarizing all Claude projects.

## Usage

1. Call `/workspace-dashboard`.
2. The agent will load the `workspace-dashboard` skill to display the current `DASHBOARD.md`.
